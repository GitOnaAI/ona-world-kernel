Solid PR, and the determinism story holds up under checking. `rng.chance` is `next() < p`, so forcing the crit with `chance(1)` draws the exact same value and just returns true, draw order unchanged. I ran the guards on the head: `tests/architecture.test.ts` green, the 159 parity goldens pass unmodified, and `tests/talent_primitives.test.ts` + `tests/ability_tooltip_talents.test.ts` pass.

What I liked: the free-charge billing is placed at each real cost site rather than one blanket spot (queue time for on-swing via `queuedOnSwingFree`, cast start for channels and instants, completion for cast-time spells through `applyAbility`, and the conjure/revive early returns), and every runtime reset of `queuedOnSwing` also clears `queuedOnSwingFree` (roster, arena, fiesta, death, swing), so no stale flag survives a respawn. Content-unused checks out too: nothing in `src/sim/content/` grants the new effects, so nothing player-visible moves. The `empower_next.ts` split as a thin consume-helper and the interrupt's per-player-then-global (`ABILITIES[...]`) def fallback so future mob casters are interruptible are both nice.

A few small things, all non-blocking:

- nit (confirm intent): `casting_lifecycle.ts:364` consumes `next_cast_free` at queue time for an on-next-swing ability, but toggling that same ability back off before the swing (`casting_lifecycle.ts:366` / `auto_attack.ts:139` delete the flag) burns the charge for nothing. No live impact today since no content grants it, and it matches the documented "consumed at queue time" model, but worth confirming that is the feel you want before a later PR wires `next_cast_free` onto an on-swing ability.
- nit: `queuedOnSwingFree` is correctly server-local and off the wire (it joins the other optional transients absent from `blankEntity`), and the three new aura kinds ride the existing string `kind` field so they round-trip once content grants them. No change needed, just noting I checked the parity side.
- nit: the description says "38 unit tests in tests/talent_primitives.test.ts"; the file has 14 `it()` cases. Coverage across the three primitives is fine, only the count is off.
